import 'package:flutter/material.dart';

import '../widgets/home_screens_item/home_page_item.dart';

class HomePage extends StatelessWidget {
  const HomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return HomePageItem();
  }
}
